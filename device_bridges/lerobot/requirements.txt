# Runtime dependencies used directly by the LeRobot bridge and its local data helpers.
Pillow>=10.0.0
numpy>=1.26.0
scipy>=1.12.0
pyarrow>=16.0.0
h5py>=3.11.0
pydantic>=2.8.0
PyYAML>=6.0.0

# The LeRobot CLI, robot/camera drivers, Isaac environments and optional W&B
# service remain environment-provided integrations selected by configs/lerobot.yaml.
