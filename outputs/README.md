# Model and Training Outputs

This directory stores model checkpoints, LeRobot training outputs, rollouts, and
other heavy generated files.

Contents are ignored by Git. Push model checkpoints to a dedicated model store
or attach them as release artifacts only when needed.
