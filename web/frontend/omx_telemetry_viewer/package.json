{
  "name": "atr-omx-telemetry-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Local Three.js OMX pose and policy telemetry cards for the ATR Live GUI",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=iife --global-name=ATRRobotTelemetryBundle --legal-comments=linked --outfile=../../static/omx_telemetry_viewer.bundle.js"
  },
  "dependencies": {
    "three": "0.177.0"
  },
  "devDependencies": {
    "esbuild": "0.25.5"
  }
}
