# Runtime Runs

This directory stores generated run logs, experiment traces, STL/G-code outputs,
and live workflow artifacts.

Contents are ignored by Git. Preserve important run outputs externally or attach
them to a release/artifact store rather than committing them to the source repo.
