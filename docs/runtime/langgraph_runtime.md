# LangGraph Runtime

## Current Runtime

ATR now uses a config-driven LangGraph runtime for the main closed-loop execution path.
The public `orchestrator.run_loop.RunLoop` import remains as a compatibility alias, but it points to `LangGraphRunLoop`.

Runtime chain. `OrchestrationGraph` is no longer injected into this path; YAML graph config is the execution source of truth:

```text
FastAPI Controller
  -> LangGraphRunLoop
    -> selected graphs/configs/*.yaml
    -> HandlerRegistry allowlist
    -> ATRLangGraphCompiler
    -> LangGraph compiled graph
    -> Stage Agent / ToolRegistry / Device Bridge
    -> Standard runtime event stream
```

## Canonical Graph Config

- Primary executable graph: `graphs/configs/atr_closed_loop.yaml`
- Workspace graph templates: `graphs/configs/printer_pipeline.yaml`, `graphs/configs/lerobot_pick_place.yaml`, `graphs/configs/utm_test_flow.yaml`
- Lightweight GUI metadata mirror: `configs/langgraph.yaml`
- `GET /api/graphs` discovers every `graphs/configs/*.yaml` file and returns the primary graph first. Runtime IDE graph selection is therefore config-driven instead of hard-coded to one YAML file.
- `POST /api/graphs/{graph_id}/run` starts any compile-valid graph through the shared `LangGraphRunLoop`. The primary `atr_closed_loop` graph is the normal live loop; workspace templates can be executed in test/replay/fault-injection mode and require `metadata.executable_from_runtime_ide=true` before live mode is allowed. Live runs require a successful `/api/graphs/{graph_id}/dry-run` record whose digest matches the active graph config; otherwise the API returns `GRAPH_DRY_RUN_REQUIRED`. The legacy `/api/run/start` live path applies the same primary-graph dry-run gate.
- Graph API:
  - `GET /api/graphs`
  - `GET /api/graphs/{graph_id}`
  - `PUT /api/graphs/{graph_id}`
  - `GET /api/graphs/{graph_id}/versions`
  - `GET /api/graphs/{graph_id}/versions/{version_id}`
  - `POST /api/graphs/{graph_id}/validate`
  - `POST /api/graphs/{graph_id}/validate-draft`
  - `POST /api/graphs/{graph_id}/compile` returning `compiled_graph` executable summary
  - `POST /api/graphs/{graph_id}/export-yaml` for canonical graph YAML download from the active/draft payload
  - `POST /api/graphs/{graph_id}/import-yaml` for parse + validate + compile-check without activation
  - `POST /api/graphs/{graph_id}/dry-run` with optional `start_stage` and optional draft `graph` payload for editor-draft simulation; active-config calls record live-gate evidence, draft calls return evidence without activating or satisfying the live gate
  - `POST /api/graphs/{graph_id}/run` returning the compile-checked graph summary with the run start response
- Handler/module API:
  - `GET /api/handlers`
  - `GET /api/modules`
  - `GET /api/modules/management-state`
  - `GET /api/modules/{module_id}`
  - `POST /api/modules`
  - `POST /api/modules/{module_id}/load`
  - `POST /api/modules/{module_id}/unload`
  - `POST /api/modules/{module_id}/validate`
  - `POST /api/modules/{module_id}/dry-run`
  - `PUT /api/modules/{module_id}`
  - `GET /api/modules/{module_id}/versions`
  - `GET /api/modules/{module_id}/versions/{version_id}`
- Run/artifact compatibility API:
  - `GET /api/runs/{run_id}`
  - `POST /api/runs/{run_id}/pause`
  - `POST /api/runs/{run_id}/resume`
  - `POST /api/runs/{run_id}/stop`
  - `GET /api/runs/{run_id}/approvals`
  - `POST /api/runs/{run_id}/approvals`
  - `POST /api/runs/{run_id}/approvals/{approval_id}/resolve`
  - `GET /api/runs/{run_id}/events`
  - `GET /api/runs/{run_id}/artifacts`
  - `GET /api/runs/{run_id}/artifact-file/{artifact_path}` for inline preview or `?download=1` attachment download

The graph config defines:

- executable nodes
- node positions used by the Runtime IDE graph editor
- runtime edges used by LangGraph dispatch/step completion
- logical transition edges used by the Runtime IDE and YAML audit trail
- handler ids
- stage dispatch mapping
- configured transitions that control actual next-stage selection
- terminal stages
- safety metadata

Graph compile/check responses include a `compiled_graph` summary with entry node, finish nodes, executable edges, logical transition edges, stage dispatch, and configured transitions. The Runtime IDE displays this summary after validate/compile/dry-run/import/save so visual graph edits can be compared against the runtime graph that was actually compile-checked. Successful compile-checking endpoints (`validate-draft`, `compile`, `import-yaml`, `save`, `dry-run`, and `run`) emit `graph.compiled`; schema/handler/compile failures emit `graph.validation_failed` with the action and errors in the payload.

Graph validation is an execution gate, not only a YAML schema check. It rejects missing/unregistered handlers, unknown modules, duplicate nodes/stages, stage-dispatch/node-stage mismatches, agent nodes without modules, dispatch routes that do not target the configured stage node, dispatched nodes without a finish edge, logical transition metadata that does not match `stage_dispatch`, missing default logical edges for `graph.transitions`, unguarded transition cycles, and Guardian-required graphs without terminal stop/error routes. Handler registry validation also inspects every referenced handler without executing it and rejects callables that cannot be invoked as `handler(runtime_state)`, including no-argument handlers, handlers with multiple required positional arguments, or handlers with required keyword-only arguments. `/api/handlers` exposes `handler_metadata` with module, qualname, signature, async flag, runtime-state compatibility, and signature errors, and compiled graph summaries include per-node handler signature evidence for the Runtime IDE inspector. This keeps the Runtime IDE's visual/logical route map and handler binding aligned with the compiled runtime order before any saved test or live run is allowed.

Graph dry-run responses also include per-stage execution evidence: `graph_handler`, normalized `module_id`, `module_handler`, `effective_handler`, and a sanitized `module_runtime` summary with `pre_execution_count`, `internal_graph_count`, `tool_count`, and the configured module step sequence. Successful active-config dry-runs store an in-memory `dry_run_record` containing the graph digest, timestamp, start stage, max steps, step count, and compiled graph summary. This makes config edits auditable before a real run: if a graph transition or module handler changes, the dry-run output must show the changed route and handler before activation, and live execution is blocked until the stored digest matches the active graph config.

Activation is execution-affecting, not only a UI state. `PUT /api/graphs/{graph_id}` with `activate=true` writes the validated graph payload back to the active YAML under `graphs/configs`, records a new dry-run gate for that exact digest, and subsequent `/api/graphs/{graph_id}/dry-run` and `/api/graphs/{graph_id}/run` calls compile from that active YAML. Unit coverage `test_activated_graph_version_becomes_saved_run_target` uses an isolated active graph store, changes `design -> guardian`, saves with activation, then verifies the persisted YAML, dry-run sequence, live gate digest, and run compile summary all use the activated transition.

## Logical Transition Candidates

Runtime routing is no longer limited to one visual arrow per stage. `graph.transitions` remains the default path for backward compatibility, but `graph.edges[*].metadata.runtime_edge=logical_transition` may define multiple candidate routes from the same stage. `GraphConfig.next_stage()` evaluates those candidates at runtime using the current stage result and runtime context before falling back to the default transition.

Supported candidate metadata:

- `from_stage` / `to_stage`: stage-level source and target used by the runtime.
- `condition` or `transition_condition`: route condition such as `default`, `guardian_decision:stop`, `guardian_decision:error`, `decision:<value>`, or `next_stage:<stage>`.
- `default_transition`: marks the candidate that mirrors `graph.transitions[from_stage]`.
- `condition_key` / `condition_value`: optional dotted-key lookup against runtime metadata for structured conditions.
- `auto_ports`: lets the Runtime IDE recompute the nearest visual connection ports after node movement.

Runtime events emitted as `stage_transition` include `transition_candidates` and `selected_transition`, so the GUI can show not only that a node moved to the next stage but also which logical candidate caused it. In the default graph, Guardian has three actual runtime candidates: `continue -> design`, `stop -> complete`, and `error -> error`.

## Legacy Compatibility Boundary

`orchestrator.graph.OrchestrationGraph`, `orchestrator.transitions.default_next_stage`, and `orchestrator.router.stage_to_agent` are retained only for old imports and documentation compatibility. They no longer carry independent hard-coded stage order or stage-agent maps: transition lookup is derived from `graphs/configs/*.yaml`, and stage-agent lookup resolves the graph node plus `graphs/modules/<module>/module.yaml` handler. `MainController`, `RunLoop`, Live GUI planning handoff, and `/api/graphs/{graph_id}/run` do not inject those shims as runtime source of truth. New execution order or handler changes must be made in graph/module YAML and validated/compiled through the Runtime IDE/API.

## Module Runtime Application

`LangGraphRunLoop` loads `graphs/modules/*/module.yaml` for every stage referenced by the active graph. The module file is not only a UI document: the runtime wraps `AgentContext` with a stage-scoped `ModuleRuntimeContext` before calling `BaseAgent.run(state, ctx)`.

Applied at execution time:

- `module.handler`: resolves the actual `AgentRegistry` entry for the stage. The runtime uses `module.handler` first, then the graph node handler. It no longer falls back to the legacy stage map; missing or non-agent handlers fail safely as `routing_error` / `node.failed` so graph/module config remains authoritative.
- `module.llm_role`: overrides the task route used by `ctx.complete(...)` for that stage.
- `module.llm.backend`: selects the backend branch such as `vllm` or `ollama` without changing global backend selection.
- `module.llm.model` / `module.llm.primary`: overrides the primary model selected by the router.
- `module.llm.fallback`: overrides the fallback model for the stage.
- `module.prompt.system`: overrides the system prompt for the stage.
- `module.prompt.developer`: is prepended as module-scoped developer guidance to the user prompt because current backends expose a system/user prompt interface.
- `module.timeout_s`: applies to the stage LLM call when the agent does not pass an explicit timeout.
- `module.retry.max_attempts`: overrides the global retry budget for that stage. This is the number of retry attempts after the first failed execution.
- `module.retry.backoff_s`: waits before the next retry attempt after emitting the `node.retrying` event.
- `module.pre_execution`: declares allowlisted pre-stage agent steps that run before the stage handler. Design uses `orchestrator_plan` here, so the former Design-head orchestration call is config-owned instead of hard-coded in the run loop. Each step may set `output_key` to persist its result under `state.run_metadata`.
- `module.internal_graph`: declares the module's runtime step sequence. Steps without an explicit `handler` execute as checkpoint steps and emit `module.step.started/completed` without calling devices or agents. Steps with an explicit allowlisted `agent.*` handler execute that agent as an internal module step before the main module handler; their output is stored under `state.run_metadata.module_step_results[stage][step_id]`, and `active_module_step` is visible while the step runs. The main module handler still runs once after internal steps, preserving existing agent behavior while making handler-configured internal steps executable.

Emitted in runtime events/state:

- `agent_started.payload.module_runtime`, including `module_id`, `label`, `handler`, and `effective_handler`
- `agent_result.payload.module_runtime`, including the same module identity and handler evidence
- `state.run_metadata.module_runtime[stage]`
- `module.pre_step.started`, `module.pre_step.completed`, and `module.pre_step.failed` events for configured `module.pre_execution` steps; optional legacy `event_type` aliases such as `orchestrator_plan` may also be emitted for existing GUI compatibility.
- `module.step.started`, `module.step.completed`, and `module.step.failed` events for every configured internal module step. Event payloads include `executable` and `handler_configured` so the GUI can distinguish no-op checkpoints from handler-backed steps.

Enforced at execution time:

- `module.tools`: wraps the shared `ToolRegistry` with a stage-scoped allowlist. `ctx.tools.list_tools()` exposes only declared tools and `ctx.tools.call(...)` raises `PermissionError` when the agent calls a tool not listed in the active module.
- `module.safety.requires_human_approval`: blocks the stage before the Python agent handler runs, emits `approval.requested`, records the pending gate in `state.run_metadata.runtime_approvals`, and pauses the run. Resolving the approval through the Runtime IDE/API with `approved` resumes the same stage; `rejected` or `cancelled` moves the run to `error`.

Still delegated to existing safety layers:

- Device-specific live execution checks remain inside printer, LeRobot, Windows equipment, CAE, and Guardian code. The module approval gate is an additional graph-level operator gate, not a replacement for device bridge safety.

## Execution Order

Default closed-loop order:

```text
idle
  -> design
  -> specimen
  -> vision
  -> manipulation
  -> equipment
  -> analysis
  -> knowledge
  -> bo
  -> guardian
  -> design | complete | error
```

`bo` is now a mandatory configured graph stage between `knowledge` and `guardian`, not a hidden sidecar.

## Live GUI Planning Handoff

The Live GUI planning handoff also uses the configured LangGraph runtime for stage execution. Design and Specimen stages are executed with `RunLoop.step()` in a planning single-step mode, and the post-specimen tail is derived from the active `graphs/configs/*.yaml` transition table instead of a hard-coded stage list. In the default graph this tail is `vision -> manipulation -> equipment -> analysis -> knowledge -> bo -> guardian`. This preserves the chat-specific planning messages while ensuring module handler selection, LLM/prompt overrides, tool allowlists, retry policy, human approval gates, module `pre_execution`, and standard runtime events are applied consistently. Planning may pass `run_orchestrator_before_design=False` only when the chat handoff has already obtained an orchestrator-approved plan, preventing duplicate Design pre-stage calls while keeping the default runtime config-owned.

Live GUI planning messages have a renderer contract in addition to the backend event contract. `analysis_ai` messages that include `fem_artifacts.contour_url` render the CAE/FEM contour card directly in the chat, and `bo_ai` messages that include `bo_result.benchmark.strategies.<strategy>.surrogate_trace` render the BO surrogate/acquisition SVG plot plus selected candidate rows. The controller copies CAE contour/report files into `/api/planning/artifacts/{run_id}/{specimen_id}/...` before appending the analysis message, and the BO message carries the plot-ready `bo_result` payload produced after Knowledge Agent context is merged. `tests/ui/planning_browser_audit.py` opens `/live` in a real browser and verifies both cards are present, so regressions where the loop computes BO/CAE data but the chat fails to display it are caught before merge.

Dedicated workspace APIs such as `/api/bo/run`, `/api/cae/*`, `/api/lerobot/*`, `/api/printer/*`, and `/api/equipment/windows/*` remain direct device/workspace controls; they are not the main closed-loop run path. They now emit Runtime IDE-compatible events through `MainController.emit_workspace_result(...)` so direct workspace actions still appear in the same run trace, timeline, and artifact lineage model as graph runs. Each workspace execution also writes a run-local `workspace/<workspace>/*_result.json` evidence file under the current run directory. Local file paths returned by tools, such as CAE input/report/contour files, are copied into that same run-local workspace folder when they are small enough to preview; oversized files are represented by pointer JSON rather than silently disappearing from lineage.

## Dedicated Workspace Runtime Events

Direct workspace APIs preserve their existing response shapes, but execution-like actions emit standard runtime aliases:

- BO benchmark/run: `tool.completed` and, for agent-mediated BO, `node.completed` with `node_id=bo` and `module_id=bo`. BO workspace runs additionally create a compact `workspace/bo/*_bo_progress.svg` artifact from best-score curves and the latest acquisition trace so the Runtime IDE can preview BO progress without opening raw JSON.
- CAE run: `tool.completed` plus `node.completed` with `node_id=analysis` and `module_id=analysis`. CAE input/report/contour files are copied from `artifacts/cae` into `runs/<run-id>/workspace/cae/` and linked by `artifact.created` events.
- LeRobot and Manipulation Agent bridge actions: `tool.completed` and, for agent bridge run/test, `node.completed` with `node_id=manipulation`.
- Printer autoejection test: `tool.completed` with `node_id=specimen`.
- Windows PyAutoGUI bridge test/run-program: `tool.completed` and, for macro execution, `node.completed` with `node_id=equipment`.
- Artifact-like fields such as `log_path`, `dataset_path`, `checkpoint_path`, `ejection_gcode_path`, `preview_url`, and `report_url` also emit `artifact.created`.
- Runtime IDE groups run-local workspace artifacts by producer stage: `workspace/bo/...` under `bo`, `workspace/cae/...` under `analysis`, `workspace/printer/...` under `specimen`, `workspace/lerobot/...` under `manipulation`, and `workspace/equipment/...` under `equipment`.

Each workspace event carries `payload.module_runtime.direct_workspace_api=true`; this distinguishes manual workspace controls from the main compiled graph while keeping the same frontend event contract. The same payload includes `runtime_artifacts` when files were materialized under the run directory, allowing `Related Artifacts`, `Artifact Lineage`, `Artifact Preview`, `Trace Event`, and `Replay Producer Stage` to work for direct workspace API calls as well as compiled graph runs.

Closed-loop graph runs also materialize runtime evidence for stages that otherwise produced only nested payloads. BO stage results write `runs/<run-id>/runtime/bo/*_bo_agent_result.json` plus `runtime/bo/*_bo_progress.svg` from the benchmark/acquisition trace. Analysis stage results write `runs/<run-id>/runtime/analysis/*_analysis_agent_result.json` and copy CAE input/report/contour files from `artifacts/cae` into the run directory. These files are emitted through `artifact.created` events and stored in `state.run_metadata.runtime_artifacts`, so the Runtime IDE can show BO plots and CAE contour/report outputs for the main 5-cycle closed-loop run, not only for dedicated BO/CAE workspace buttons.

## Safety Boundary

The GUI and future graph editor must not import arbitrary Python code.
Graph configs may only reference handlers registered in `HandlerRegistry`.
A graph must validate and dry-run before live execution. The runtime API enforces this for `/api/graphs/{graph_id}/run` and `/api/run/start` in live mode using the active graph digest recorded by the latest successful dry-run.

Current implementation preserves existing live/test safety gates inside agents and device bridges:

- printer live gates remain in Prusa bridge/tool code
- LeRobot live gates remain in LeRobot bridge/tool code
- Windows equipment live gates remain in PyAutoGUI bridge/tool code
- Guardian remains the graph-level safety review node

## Event Schema

Runtime events preserve the existing GUI/test keys. The controller emits `run.created` as soon as a run id, experiment id, graph id, logger bundle, and initial state are allocated; the LangGraph run loop then emits `run.started`, node/module/tool/artifact events, and terminal run events. Controller-origin Runtime IDE events such as `run.created`, workspace `tool.completed/tool.failed`, workspace `node.completed/node.failed`, workspace `artifact.created`, and forced control events are persisted to the same structured JSONL run log before being broadcast to SSE subscribers.

- `event_type`
- `level`
- `timestamp_stage`
- `payload`
- `state`

They also include Runtime IDE aliases:

- `type`
- `severity`
- `ts`
- `graph_id`
- `node_id`
- `module_id`
- `agent`
- `status`

`type` follows the Runtime IDE schema where possible, for example `run.created`, `graph.compiled`, `graph.validation_failed`, `run.started`, `node.started`, `node.completed`, `node.failed`, `node.retrying`, `edge.traversed`, `artifact.created`, `module.graph.started`, `module.step.planned`, `module.step.started`, `module.step.completed`, `module.step.failed`, `module.graph.completed`, `module.graph.failed`, `run.completed`, `run.failed`, and `run.stopped`. The legacy `event_type` remains for current dashboard compatibility.

When an agent output includes artifact-like keys such as `artifact`, `artifacts`, `specimen_artifacts`, `fem_artifacts`, or URL/path fields, `LangGraphRunLoop` emits additional `artifact.created` events so the Runtime IDE can refresh artifact lineage without parsing every agent-specific payload. Dedicated workspace artifacts follow the same event type, are written to the structured JSONL log via the controller event publisher, and are materialized under `runs/<run-id>/workspace/<workspace>/` whenever the workspace response contains local files or plot-ready BO data.

## Implementation Files

- `graphs/schema.py`: graph and module config schemas (`GraphConfig`, `ModuleConfig`, module step/LLM/retry/safety/prompt models)
- `graphs/registry.py`: allowlisted handler registry
- `graphs/validator.py`: config validation
- `graphs/compiler.py`: LangGraph compiler
- `graphs/version_store.py`: graph config version store
- `graphs/module_store.py`: module config version store
- `graphs/modules/*/module.yaml`: editable module-level internal graph configs
- `orchestrator/langgraph_runtime.py`: runtime execution engine
- `orchestrator/run_loop.py`: compatibility alias
- `graphs/configs/atr_closed_loop.yaml`: default executable graph
- `graphs/configs/printer_pipeline.yaml`: 3DP/printer workspace graph template
- `graphs/configs/lerobot_pick_place.yaml`: LeRobot manipulation workspace graph template
- `graphs/configs/utm_test_flow.yaml`: UTM/equipment-analysis workspace graph template

## Migration Rule

Do not remove agent modules or device bridges during graph work.
Refactor execution wiring through graph config and registry first; agent behavior remains behind `BaseAgent.run(state, ctx)`.

## Runtime IDE Save Rules

- Active graph edits are rejected while a run is active.
- Graph payloads must validate against `GraphConfig`.
- Graph handlers must exist in the allowlisted `HandlerRegistry`.
- Draft validation performs validate + compile without writing a version. Graph save performs validate + compile before writing active YAML.
- Graph validator checks missing nodes, missing handlers, duplicate node ids, transition targets, disconnected executable nodes, unknown module references when module ids are supplied, graph safety metadata such as `guardian_required`, and unguarded transition cycles. Guardian-mediated closed-loop cycles are allowed.
- Runtime IDE transition edits update `graph.transitions` only when the operator explicitly sets a default transition. Port-drag connections preserve the existing default and add a condition-scoped logical candidate edge unless no default exists yet. Non-default port-drag candidates use the runtime-valid `next_stage:<target>` condition, so a later stage result can request the candidate target through `state.run_metadata.next_stage`, `requested_next_stage`, or `agent_result.next_stage`. The compiler ignores logical transition edges as executable LangGraph edges, but `GraphConfig.next_stage()` evaluates them as actual runtime routing candidates.
- Module validation first parses the payload through `graphs.schema.ModuleConfig`, then applies runtime allowlist checks for registered handlers and tools. It checks module id, registered handler, pre-execution list shape, pre-execution handlers, internal graph list shape, duplicate internal step ids, internal step handlers, LLM config shape, prompt config shape, timeout/retry bounds, safety flags, tools shape, and registered tool names. Runtime execution uses `retry.max_attempts`, `retry.backoff_s`, and `pre_execution`; invalid values are rejected before save.
- Module `tools` is an execution allowlist. Keep it synchronized with real agent calls such as `geometry.generate_metamaterial_stl`, `experiment.evaluate`, `lerobot.rollout.start`, `equipment.pyautogui.run`, `cae.run_static_analysis`, and `device.health`; otherwise validation rejects unknown tools or the stage fails fast with `PermissionError`.
- Module dry-run returns the configured `pre_execution` steps followed by the internal module step sequence without calling tools/devices. Each step includes `handler_configured` and `executable`; only internal steps with an explicit `agent.*` handler are executed as internal handler calls during a real run, while omitted handlers remain checkpoints. The response also includes a `summary` block with step counts, pre/internal counts, executable/checkpoint counts, handler count, ordered step ids, first step, and last step. Graph dry-run embeds the same module evidence under each stage's `module_runtime` block, including effective handler and phase counts, so graph-level route checks and module-level step checks can be compared before save/activation. During real stage execution, pre-stage steps emit `module.pre_step.*` events and internal graph steps emit planning/start/completion/failure events for timeline and replay.
- Runtime IDE module handler and internal-step handler dropdowns are populated from `GET /api/handlers`; operators cannot select arbitrary Python import paths. The allowlist is built from fixed runtime control handlers plus every agent registered in `AgentRegistry`, not only handlers already present in the active graph. The saved module handler is runtime-active and changes which registered agent is executed for that stage.
- Module saves may update `graphs/modules/<module_id>/module.yaml`, but only when the module handler is registered. `LangGraphRunLoop` reads module YAML from the graph module root at construction time, so an activated module save changes the effective handler, LLM hints, prompt/tool allowlist, retry policy, safety flags, and module step sequence for the next run. Unit coverage `test_activated_module_version_changes_runtime_handler` saves `design.handler=agent.guardian_agent` through the module API in an isolated module store, then proves the next RunLoop step executes `guardian_agent` for the `design` stage while `design_agent` remains unused.
- Fresh module graph tabs open with a clean draft state and missing validation/dry-run evidence, not as modified drafts; the strip changes to dirty only after the operator edits handler, prompt, tool, LLM, step order, or raw module JSON.
- Module Designer is hosted in the standalone `/module-management` page, not inside the Runtime IDE graph/config editor. It writes an uploaded original source and a Gemma 31B-transformed ATR adapter under `graphs/modules/<module_id>/`, stores paths in module metadata, and still binds execution to an allowlisted handler. Arbitrary uploaded Python is never imported or executed by the GUI just because it exists on disk. Operators must press `Register Generated` or run `atr module register-generated <module-id>` to approve a generated adapter. That API statically checks `handler.py`, rejects blocked imports/top-level side effects, changes the module handler to `module.generated_adapter`, clears `pending_handler_registration`, removes placeholder `runtime.step_complete` internal-step handlers left by the unapproved staging phase, saves a version, and only then allows the LangGraph runtime to load the adapter through the generated-module wrapper.
- Graph and module saves write immutable snapshots under `memory/graph_versions` and `memory/module_versions`.

## Runtime IDE GUI

- Route: `/ide`
- Static frontend: `web/templates/runtime_ide.html` + `web/static/runtime_ide.js`
- Main GUI entry: LangGraph Runtime Map -> Open Runtime IDE
- Current GUI supports multi-graph selection from every `graphs/configs/*.yaml`, Runtime Top Bar status, Graph Explorer search/list, categorized Module Library drag/drop, SVG node icons from the Runtime IDE asset set, practical Node Inspector cards, selected-node auto-repair when switching graph/module tabs, port-based transition editing, condition-scoped multi-edge candidates, nearest-port auto routing after node movement, graph-first startup with heavy Activation/Run Launcher controls folded into compact operator drawers, automatic initial graph fit, canvas viewport coverage feedback with a `Fit Graph` overview control, accessible graph tabs with visible `ACTIVE`/`DRAFT` badges, current canvas context labels, DOM dataset state, `window.atrRuntimeIdeState` debug snapshots for GUI/CUI verification, active-baseline route diff cards for draft edge edits, trash-zone node removal, validate draft, compile with executable graph summary, dry-run, graph YAML export/import, graph save, graph version history draft loading, handler listing, and a hidden compatibility module-config host used only to keep legacy JS/API bindings stable. Module lifecycle/config editing is intentionally owned by the Module Management Tool.
- Graph Version History uses `/api/graphs/{graph_id}/versions` and `/api/graphs/{graph_id}/versions/{version_id}`. Loading a version puts that saved graph into the editor draft only; operators must validate, dry-run, and save before it becomes active.
- Runtime Readiness sits at the top of the Bottom Dock and summarizes whole-graph execution readiness: node/edge/route counts, handler allowlist coverage, module linkage and pending-registration state, validation/compile/dry-run evidence, live-gate status, and device/backend warnings. Its issue rows select the affected graph node when possible, and its action buttons call the same guarded Runtime API paths as the toolbar (`Validate`, `Dry Run`, and `Run Launcher`). When an internal module graph tab is active, Runtime Readiness switches to module-draft semantics: evidence is read from the selected module draft preflight record, live gate is shown as `n/a` / `module draft only`, graph Run Launcher is hidden, and the action row exposes `Validate Module`, `Dry Run Module`, and `Save Module`. Handler/module catalog coverage is not marked as registered until `/api/handlers` and `/api/modules` have loaded; while those catalogs are pending the panel shows explicit loading warnings and refreshes graph badges after catalog load. The same readiness evidence is projected onto graph nodes as `readiness-ok`, `readiness-warn`, or `readiness-error` with compact node badges, so missing handlers/modules/routes and pending generated-module registration are visible directly on the canvas. Route coverage follows graph semantics: entry nodes do not require incoming logical routes, and finish/terminal nodes do not require outgoing logical routes; module internal graph tabs derive those entry/finish markers from the first and last configured module steps. Clicking a readiness issue focuses the appropriate repair surface: handler/runtime issues focus Node Inspector and Runtime Recovery, route coverage issues focus the Transition Editor and preselect the current/baseline default target when available, and module/registration issues focus the module tools entry point. This gives operators a single operational preflight view before they inspect individual nodes. The always-visible Draft safety gate above the canvas is kept as a compressed status-chip strip so validation/dry-run/save/live-gate evidence remains visible without pushing the graph canvas below the first viewport.
- Draft Safety Strip sits directly above the graph canvas and summarizes the active tab gate state at all times: graph/module draft dirty state, route diff, full config fingerprint diff, validation evidence, dry-run evidence, save/live-gate state, and the next operator action. Its primary action button executes the same guarded Runtime API path as the toolbar (`Validate`, `Dry Run`, `Save Version`, `Record Gate`, or `Open Run Launcher`) so the strip is an operational gate, not a passive warning banner. This is intentionally always visible because the saved active graph, not an unsaved editor draft, is what run buttons execute.
- Activation Checklist shows draft validation, compile, dry-run, and saved-version evidence as expandable operator evidence cards. The cards include compiled graph summary, default routes, conditional candidate routes, full graph config fingerprint status, stage-dispatch mapping, dry-run payload type, live-gate/digest status, and a stage/next/node/handler sequence table. Runtime IDE dry-run sends the current editor graph payload to `/api/graphs/{graph_id}/dry-run`, so draft/version-loaded graph changes are simulated before save. Draft dry-run evidence is not recorded as the live gate. `Save Version` repeats the same non-device dry-run on the server; when the save activates the graph, the returned dry-run record is also registered as the backend live-run gate for that exact config digest.
- Run Launcher starts the saved active graph through `/api/graphs/{graph_id}/run`. Its drawer now begins with a Saved Active Graph Execution summary that repeats the exact target, selected mode, required action, and blocker text before the operator reaches run buttons. The run buttons are labeled `Run Saved Test` and `Run Saved Live` to make it explicit that unsaved editor JSON is never executed directly. Every run mode is blocked when the editor contains unsaved draft route/config differences, because test/replay/fault/live all execute the active graph config rather than the unsaved editor draft. The browser compares both logical route records and the full normalized graph config fingerprint, so non-route edits such as handler, module, model, timeout, retry, safety metadata, or YAML import changes cannot silently run against the wrong saved graph. Live mode additionally requires the operator confirmation checkbox and a backend active dry-run gate. The Runtime IDE reads `/api/graphs/{graph_id}/dry-run-gate` to show run preflight status and blocks the run button in the browser before the request is sent when any required preflight item is missing. The launcher shows an execution-target strip (`Saved active graph`, graph id, mode, live gate) and disables `Run Saved Test`, `Run Saved Live`, or `Record Active Dry-run Gate` whenever that specific action would target the wrong graph state. The separate `Record Active Dry-run Gate` action intentionally calls dry-run without a draft payload when the operator wants to refresh gate evidence without saving a new version.
- Node Inspector is structured for operator use: Info, Runtime, Runtime Recovery, Module Step Trace, Runtime Routes, Config, I/O Contract, and Code Mapping cards show graph identity, current/visited/failure status, agent run status, outgoing/incoming default and candidate routes, graph/module/effective handler mapping, handler signature/runtime-callable evidence from `/api/handlers`, model/timeout/retry/safety config, recent node-scoped events, stage-scoped module step trace events, declared `io_contract` input/output, captured input/output payload previews, schema mismatch indicators from runtime events, module config path, prompt path, Python source path, transformed ATR adapter path, and registry state. A top `Node Quick Actions` card keeps the most common selected-node operations visible without scrolling: Dry-run from node, Validate Draft, Open Module Management, and Inspect latest issue. Runtime Routes rows let the operator select an edge into the transition editor or dry-run from the route source without hunting for thin SVG strokes. The Runtime Recovery card summarizes node-scoped failed/warn events, schema contract status, missing routes, module trace failures, and pending handler registration, then exposes the same recovery actions deeper in the audit trail. The Code Mapping card is read-only and includes a node-scoped dry-run shortcut; it never edits Python source directly.
- Module creation, operational module loading/unloading, and module configuration editing are integrated into `/module-management` as the **Module Management Tool**. Runtime IDE opens it in a new window from the Module Library area. Module Management graph-usage rows open `/ide?graph=<graph_id>&node=<node_id>` so operators can jump directly from a module draft to the affected runtime graph node. The Module Library rows are select-only and show loaded/select-only badges; load/unload operations live in the Workbench action row, and loaded-strip chips only refocus already loaded modules. The management load/unload state is a workspace state for operator module management and does not delete module files or change graph/module config by itself. Module config save still writes versioned `module.yaml` through the runtime module API.
- Bottom Dock includes Live Runtime, Agent Status, Device Status, Metrics, Human Approval Queue, Run Timeline, Artifact Lineage, Artifact Preview, Replay Preview, and Event Log panels. The dock uses compact rows with internal scroll regions so it stays close to the graph canvas instead of pushing the operator into a long dashboard page. Agent/device/metric views are derived from `/api/state`, buffered run events, and SSE events. `/api/state` now includes a short-lived `system_resources` snapshot with host RAM and NVIDIA GPU/VRAM telemetry when `nvidia-smi` is available; the Top Bar health badge, Device Status panel, and Metrics panel display RAM/VRAM utilization without changing the runtime event schema. The Event Log merges structured runtime events with IDE operator actions, supports `all`, `info`, `warn`, and `error` filters, and exposes an `Inspect` action on runtime rows so operators can jump directly from a warning/error log entry to the selected event detail, matching the Runtime IDE event-schema requirement without changing backend event payloads. Human Approval Queue is event-backed and connected to run-scoped approval request/resolve APIs; approval-related remediation cards include a `Focus Approval Queue` action that scrolls to and highlights the matching pending approval item before the operator approves or rejects it. After an approval is resolved, the selected event remains focused, the remediation card switches its approval badge from pending to resolved with decision/operator evidence, and the queue refreshes from the same run-scoped approval source. Operator decisions are recorded as `approval.requested` and `approval.resolved` events and, for module safety gates, are also applied to `state.run_metadata.runtime_approvals` so the paused LangGraph stage can resume or fail safely.
- The GUI consumes `/api/events/recent` and `/api/events/stream` so active stages, active transition edge, visited nodes, recent node payloads, and agent status snapshots are reflected in the Runtime IDE dashboard.
- The GUI loads `/api/state`, `/api/runs/{run_id}/events`, and `/api/runs/{run_id}/artifacts` to show run status, run id, elapsed time, active agent/stage, active module step, device health, run timeline, current-run artifact lineage, and a time-travel replay preview. The Run Timeline now includes event severity/count summaries, selected-event raw payload/state inspection, related node/handler/module metadata, module step trace panels for `module.graph.*`, `module.step.*`, and `module.pre_step.*` events, and a replay basis panel. Selecting a timeline event focuses the corresponding node, shows an operator decision strip with event status, runtime target, selected route/candidate count, replay availability, and related artifact count, shows a remediation panel for warning/error events with likely cause, impacted target, evidence, and recommended next actions, and runs a dry-run preview from that event's stage when possible. Replay Preview now adds a validation panel that compares the selected event against the dry-run first step for stage, selected route, handler, and compile status before showing the full sequence. Runtime event state from SSE is merged into the visible snapshot for live active-module-step display, but IDE control events such as `graph.compiled` and `graph.validation_failed` do not drive the active runtime stage, preventing compile/check previews from overwriting the live stage. Module trace panels first prefer the active/selected run id and fall back to same stage/module trace events when inspecting historical or replayed events.
- Artifact Lineage groups current-run files by inferred stage/run context, shows run/file counts, keeps preview/download actions, and uses artifact-related runtime events when available to make lineage easier to audit. Timeline event detail cards expose operator actions for `Focus Node`, `Replay From Stage`, and `Related Artifacts`; artifact lineage and artifact preview cards expose `Trace Event` and, when the producer stage is dispatchable, `Replay Producer Stage` so a file can be tied back to the event that produced or referenced it and dry-run replayed from that producer stage. Artifact Preview also shows a provenance strip with producer stage, handler, artifact type, preview availability, and replay availability before inline file contents. The event-stage resolver prioritizes event-local `node_id` / payload stage metadata before snapshot state so historical events do not appear under the wrong stage after the global run state has advanced or returned to `idle`.
- Top Bar Pause/Resume/Stop actions call the run-scoped Runtime API (`/api/runs/{run_id}/pause|resume|stop`) so control buttons target the active run explicitly.
- Artifact lineage entries include preview/download URLs. Text and image artifacts are previewed inline through a path-safe `/api/runs/{run_id}/artifact-file/{artifact_path}` route; other artifact types remain downloadable.
- This is an operational config surface over the Runtime API. A future React Flow editor can reuse the same backend endpoints.

## Module Management Tool

- Route: `/module-management`
- Static frontend: `web/templates/module_management.html` + `web/static/module_management.js`
- Purpose: module lifecycle and module configuration management, separated from Runtime IDE graph editing.
- Supported actions:
  - browse/search the Module Library with module icons
  - inspect selected module metadata, source/adapter paths, and graph usage/runtime impact, with one-click deep links to affected Runtime IDE graph nodes
  - show which graph nodes currently reference the selected module before the operator saves module changes
  - load or unload a selected module in the operator workspace state
  - edit the selected module handler, LLM route, backend/model override, timeout/retry policy, safety gates, prompt overrides, tool allowlist, pre-execution steps, internal graph steps, and raw module JSON
  - inspect module save preflight state before activation; form edits must be applied to the draft JSON and changed module drafts must validate and dry-run before `Save Version` is allowed in the GUI
  - use the sticky Module Configuration section shortcuts (`Summary`, `Steps`, `Evidence`, `Raw JSON`) to jump through long module configs without losing the current draft context
  - validate, dry-run, or save a selected module through the same runtime APIs used by Runtime IDE compatibility controls
  - inspect dry-run evidence showing exact draft step order, pre/internal counts, executable handler count, checkpoint count, stale-draft warnings, and current graph nodes affected by the module, with one-click Runtime IDE deep links for each affected graph node
  - inspect module version history and load a saved module.yaml version into draft without activating it
  - create a new module from a Python source file with category, handler, LLM role, and operator notes
  - open the Runtime IDE in a separate window after module management work
- Separation rule: `/module-management` owns module creation/loading/unloading and module config editing. `/ide` owns graph editing, run launch/readiness, node inspection, timeline, and artifact lineage; it keeps only a hidden compatibility config host so old JS references do not break. Loading/unloading a module is not the same as saving module YAML or activating a graph transition. Module Management uses a wide single-column configuration workspace so the Summary, typed config cards, runtime steps, dry-run evidence, and raw JSON are not split across competing IDE panes. The raw JSON editor is intentionally collapsed by default; normal operation should use the typed routing, LLM, safety, tool, and step editors first, then raw JSON only for advanced inspection or repair.

## Graph Editor Interaction

Node positions are first-class config data via `GraphNode.position`. The Runtime IDE renders nodes on a 16px snap grid, allows drag repositioning, updates the JSON/YAML payload, and keeps logical transition edges visible as curved canvas edges. The minimap, Graph Explorer, Top Bar, Inspector, and Bottom Dock reflect current node positions and active node state. These interactions still follow the safety rule that the GUI edits config only, not Python source. The initial `Fit Graph` uses a readable minimum zoom instead of shrinking the whole map until every node fits, because node labels, ports, and edge hit targets must remain usable; when the full graph does not fit at that readable zoom the canvas hint explicitly switches to `scroll/map` and operators navigate with scroll plus the minimap. The minimap is interactive: it shows the current canvas viewport rectangle, clicking or dragging empty minimap space pans the graph canvas to that world coordinate, and clicking a minimap node still selects/focuses that node.

### Edge And Module Editing

Runtime IDE uses drag/drop edge editing from node ports. Operators drag from a source port to another node or target port; the canvas shows a live connection tooltip explaining whether the drop will create a default route or add a conditional candidate, including the condition that will be written. The IDE updates `graph.transitions` plus the corresponding logical transition edge in YAML. If the source already has a default transition, the new edge is stored as a `next_stage:<target>` candidate instead of replacing the default; if no default exists, the new edge becomes the default transition. This is execution-significant, not just visual: `GraphConfig.transition_candidates(stage)` exposes all saved logical candidates, `GraphConfig.next_stage(...)` chooses `next_stage:<target>` candidates when agent output or runtime metadata requests that stage, and the LangGraph run loop records the selected candidate in `edge.traversed.payload.selected_transition`. During drag, the source node is highlighted in amber, the detected target node is highlighted in green, the status text explains whether the drop will create a default route or a candidate route, and `Escape` cancels the pending connection. Edges render with a wider transparent hitbox and clickable route labels so operators do not have to click the thin SVG stroke precisely. To keep dense maps readable, simple default edges are shown as arrows without repeated `default` labels, while conditional defaults and candidates keep visible labels. The renderer mirrors `GraphConfig.transition_candidates`: when a logical edge already represents the configured default target, the `transitions` fallback is not drawn a second time. Multi-route source nodes show a compact route-count badge. Clicking an edge, label, or route-inventory row loads source, target, condition preset, and condition value into the edge editor. The editor supports `default`, `next_stage:<target>`, `decision:<value>`, `guardian_decision:<value>`, and custom conditions; applying a changed condition replaces the selected edge condition instead of leaving stale duplicates. The route preview panel explains whether the edit changes `graph.transitions` or creates a candidate, what runtime metadata must be present for the condition to match, whether the current action updates or replaces an existing edge, and the active-baseline route/config diff that will be saved if the draft is activated. It also lists every outgoing route from the selected source stage, marking default and candidate routes so multi-route LangGraph behavior is visible before compile/dry-run. When the source changes, the editor automatically selects that source stage's current default target when one exists. `Delete Edge` removes the selected configured transition and its logical edge. Node target lookup uses DOM point lookup plus an expanded bounding-box fallback to avoid missed drops when the pointer is near icons, ports, or node edges.

The Module Library lists modules from `graphs/modules/*/module.yaml` and groups them by metadata/category, module id, handler, and tool usage. Double-clicking a main graph agent node opens that module's internal graph as a browser-like tab beside the fixed Main System tab. Runtime IDE tab activation accepts the fixed tab id, the `main` alias, full `module:<id>` ids, or plain module ids so GUI actions, browser tests, and future CUI deep links resolve the same tab. Graph/node deep links such as `/ide?graph=atr_closed_loop&node=design` select and focus the target node, then background-load that node's full module payload so the Inspector can show module config, prompt/tool, I/O, and code-mapping evidence without requiring the operator to manually open Module Management first. Internal graph tabs render `pre_execution` and `internal_graph` steps as editable graph nodes with compact `pre:<step>` / `step:<step>` labels, while preserving the full YAML step ids in config. Default module step transitions are de-duplicated when both `transitions` and logical `edges` describe the same route, so each internal step route is shown once. Internal module routes use a dedicated high-contrast `module-flow` edge style with visible step-to-step labels, and the browser audit now verifies that the SVG paths, labels, computed visibility, module-specific arrow marker, and minimum label width are present after opening an agent module tab. Internal module default layout uses wider 560px column spacing than the main graph so the step-to-step labels remain readable; arrow markers are drawn with a joined stroke-colored module marker so the arrowhead reads as part of the same route line. Dropping a catalog item on the Main System graph adds a draft graph node with `module_id=modules/<module_id>`. Dropping it on an agent internal graph adds an internal module step. Dragging a graph node over the bottom trash zone removes it from the current draft graph or module step list.

Module Designer converts uploaded Python files into ATR internal module artifacts with Gemma 31B. The API endpoint is `POST /api/modules`; with `transform_with_llm=true`, it calls `gemma4:31b` directly through the vLLM backend, requests strict JSON, writes `handler.py`, writes the original source file for audit, auto-classifies the category, filters tools against the registered tool list, and saves a versioned `module.yaml`. The saved module remains execution-safe because `handler` must still be present in `GET /api/handlers`; unregistered generated adapters are marked `pending_handler_registration` and remain bound to `runtime.step_complete`. When the operator explicitly registers the generated adapter, `POST /api/modules/{module_id}/register-generated` validates the adapter, versions/activates the module with `handler=module.generated_adapter`, removes staging-only `runtime.step_complete` internal-step handlers, and real LangGraph execution loads `async run(state, ctx)` only through the generated adapter wrapper. Unit coverage now exercises create -> register -> validate -> dry-run -> management-load so the GUI/CUI/API contract is tested as one flow.

GUI/CUI cross-compatibility is file/API based. Runtime graph editing uses the same `/api/graphs` endpoints from the browser and `atr graphs` / `atr graph show|validate|compile|dry-run|gate|export-yaml|import-yaml|save-yaml|run`, so graph YAML edits, validation gates, dry-run gates, version history, and active graph launches are reflected after reload. Module management uses the same `/api/modules` endpoints from the GUI and `atr modules` / `atr module show|validate|dry-run|load|unload|versions|version|save-yaml|register-generated|create`, so module workspace load state, active module YAML, generated-adapter approval, validation/dry-run evidence, and version history also appear after reload without a separate import/export step. Current smoke evidence should include `atr graph validate atr_closed_loop`, `atr graph compile atr_closed_loop`, `atr graph dry-run atr_closed_loop`, `atr graph gate atr_closed_loop`, `atr module validate design`, `atr module dry-run design`, `atr module load design`, and `atr module unload design` against the running server.

### Verified Runtime IDE Evidence

The current 2026-05-26 smoke set against `http://127.0.0.1:7860` passed the following checks:

- Runtime IDE full browser audit: `.venv/bin/python tests/ui/runtime_ide_browser_audit.py --base-url http://127.0.0.1:7860 --webdriver-url http://127.0.0.1:4448 --scenario all --out-dir /tmp/atr_runtime_audit_all_v89`
- Runtime IDE evidence produced under `/tmp/atr_runtime_audit_all_v89/`, including layout, evidence VCD, graph switching, canvas interactions, workspace artifact lineage, unsaved-draft run preflight, explicit discard draft, save/version lifecycle, saved test run, Human Approval Queue approve/reject, timeline replay/detail, invalid handler, invalid module, and invalid route screenshots. The `run-preflight` browser audit verifies that editing the graph JSON draft disables `Run Saved Test`, `Run Saved Live`, and `Record Active Dry-run Gate`, keeps them disabled after draft validate/dry-run until `Save Version`, and keeps Runtime Readiness warning about the unsaved draft instead of claiming the graph is executable. The `discard-draft` audit verifies that explicit `Discard Draft` restores a dirty main graph editor without writing markers to the active graph API, re-enables saved run buttons, then opens the `design` module tab and verifies a dirty internal-step position is restored in both graph JSON and module JSON. The `save-version-lifecycle` audit then edits a harmless metadata marker, validates/dry-runs, clicks `Save Version`, verifies the active graph version is updated and run buttons are restored, starts a saved test run through the UI, stops the run it created, and restores the original active graph payload.
- Planning/Live browser audit: `.venv/bin/python tests/ui/planning_browser_audit.py --base-url http://127.0.0.1:7860 --webdriver-url http://127.0.0.1:4448 --out-dir /tmp/atr_planning_audit_v87`
- Module Management browser audit: `.venv/bin/python tests/ui/module_management_browser_audit.py --base-url http://127.0.0.1:7860 --webdriver-url http://127.0.0.1:4448 --out-dir /tmp/atr_module_management_audit_v87`
- CUI graph smoke: `atr graph validate atr_closed_loop`, `atr graph compile atr_closed_loop`, `atr graph dry-run atr_closed_loop`, and `atr graph gate atr_closed_loop`
- CUI module smoke: `atr module validate design`, `atr module dry-run design`, `atr module load design`, and `atr module unload design`
- Static/regression checks: `node --check web/static/runtime_ide.js && node --check web/static/planning.js && node --check web/static/module_management.js`, `python3 -m py_compile orchestrator/langgraph_runtime.py tests/integration/test_controller_run.py tests/ui/runtime_ide_browser_audit.py tests/ui/module_management_browser_audit.py tests/ui/planning_browser_audit.py`, `.venv/bin/pytest tests/integration/test_controller_run.py tests/integration/test_bo_gui_api.py tests/integration/test_cae_gui_api.py tests/unit/test_langgraph_runtime.py tests/unit/test_module_designer_cli_contract.py -q` (`51 passed, 4 warnings`), `git diff --check`, and post-audit state check showing `is_running=false` plus no `browser_audit_save_marker` or `browser_audit_discard_marker` in the active graph metadata
- Closed-loop server smoke: `atr graph run atr_closed_loop test "closed-loop stability smoke v86"` completed as `run-20260526T065517Z-063398` with `stage=complete`, `loop_count=5`, `run.completed`, 300 runtime events, 57 artifacts, BO runtime SVGs, and CAE contour/report artifacts. Runtime IDE lineage screenshot: `/tmp/atr_runtime_closed_loop_lineage_v86/closed_loop_runtime_artifact_lineage.png`.


Module runtime steps are editable through config-only step lists. Operators can edit both `pre_execution` steps and `internal_graph` steps, reorder steps within each phase with buttons or drag/drop, duplicate/delete steps, add explicit checkpoint steps or executable agent steps, select module/step handlers from the allowlisted registry, edit prompt path/overrides, tool allowlist, LLM backend/model hints, timeout/retry policy, and safety flags, then validate/dry-run/save the module config. Internal graph tab node movement is not cosmetic: moving a step node writes the snapped position back to that step's `metadata.position` in the module JSON draft, marks the module tab dirty, and changes the Runtime IDE status to `Module Draft Changed` until validation/dry-run/save evidence is refreshed. Step cards now expose execution intent (`exec`, `checkpoint`, `disabled`, or `handler required`) and local id issues before backend validation. Blank-handler internal steps remain checkpoints even when the module handler changes; only explicit non-checkpoint internal steps receive a handler. The executable Python handler remains selected from the allowlisted registry; Python source remains immutable from the Runtime IDE. Saved `pre_execution` steps emit `module.pre_step.*` events and saved internal steps emit `module.step.planned` plus graph-level completion/failure events so the timeline reflects the active module contract.

When an internal module graph tab is active, the top-level `Validate`, `Compile`, and `Dry Run` controls operate on that module draft instead of the main graph. Each module tab keeps its own module payload snapshot, so a later background load of another module cannot make a `design` tab validate or dry-run `analysis` payload. The module catalog loader respects the active tab: automatic background loads cannot hijack an already-open module tab, and explicit module navigation from Module Management deep links opens the selected module/node context instead of silently replacing the active tab's graph. Opening a module tab starts as a clean draft with missing validation/dry-run evidence; it is marked changed only after step movement, route edits, config edits, raw JSON edits, or generated-module changes. Evidence is shown in the central `Dry-run Trace` panel and in Module Management evidence cards, not in a second visible Runtime IDE config editor: validation displays schema/allowlist status and dry-run displays pre-execution/internal step counts plus the ordered checkpoint/handler-backed step list. Runtime IDE stores a fingerprint for the validated and dry-run module draft. `Save Version` from the internal module graph tab or Module Management Tool is blocked until both evidence records match the exact current draft, and any graph/step/config/raw JSON edit invalidates the evidence. The backend save API repeats the non-device module dry-run before writing a version and returns the resulting sequence/summary, so GUI, CUI, and direct API saves all leave the same validation/dry-run evidence trail. When a module graph tab is selected, the `Activation Checklist` switches from graph live-run gates to module gates (`Validate Module Draft`, `Dry-run Module Draft`, and `Save Module Version`) so operators can see whether the current module draft is ready before pressing save. This keeps graph-tab edits auditable before a module version is saved.

### Operator Debug Plan

Before merging Runtime IDE changes, exercise these controls against the running FastAPI server and fix failures immediately:

1. Open `/ide`, confirm `Runtime Readiness` reports graph/handler/module coverage, validation evidence, dry-run evidence, live-gate status, and no horizontal page overflow. Confirm normal graph nodes render as `readiness-ok`; then intentionally load or edit an invalid draft handler/module/route in a safe test session and confirm the affected node shows a warning/error badge and the panel lists the same issue. Use its `Validate` and `Dry Run` actions once and confirm the evidence indicator changes from `---` to `VCD`. For repeatable browser evidence, run `python3 tests/ui/runtime_ide_browser_audit.py --scenario all --base-url http://127.0.0.1:7861 --webdriver-url http://127.0.0.1:4448`; it verifies graph-first layout, Validate/Dry Run evidence advancement to `VCD`, graph template switching across `atr_closed_loop`, `printer_pipeline`, `lerobot_pick_place`, and `utm_test_flow`, real canvas interactions for node drag/snap, port-to-node edge creation, minimap pan, module double-click tabs, and a saved active graph test execution through the actual `Run Saved Test` button. The saved-test audit requires no pre-existing active run, checks `run.created`, `graph.compiled`, `run.started`, and `node.started` timeline evidence, confirms canvas runtime node highlighting, then stops only the run it created. It also verifies invalid handler, invalid module, and invalid route readiness paths without saving the draft.
2. Load `atr_closed_loop`, `printer_pipeline`, `lerobot_pick_place`, and `utm_test_flow`, then confirm node canvas/minimap/JSON switch to the selected graph. Confirm the canvas view hint reports partial coverage on large graphs and `Fit Graph` keeps a readable zoom without changing graph config. Click and drag empty minimap space, confirm canvas scroll changes and the minimap viewport rectangle follows the new position. Current browser evidence: `artifacts/ui/runtime_ide_browser_audit_graph_switch.png` and `artifacts/ui/runtime_ide_browser_audit_canvas_interactions.png`.
3. Drag at least one node, validate draft, dry-run, and confirm the JSON position changes snap to the 16px grid. Browser audit currently verifies `design` moves from the active JSON position to a new 16px-snapped position and marks the main graph tab dirty.
4. Select an edge by clicking the stroke, transparent hitbox, label, or route-inventory row; edit it, drag from one node port to another node/near-edge target, cancel one pending connection with `Escape`, delete/recreate the transition, then validate draft to ensure transitions and logical YAML edges stay synchronized. Browser audit currently verifies dragging `design:right` to `vision` adds a `next_stage:vision` candidate while preserving `graph.transitions.design=specimen`. Unit coverage also verifies a saved `design -> guardian` candidate leaves the default `design -> specimen` route intact, then drives an actual LangGraph run-loop transition to `guardian` when the design agent returns `next_stage=guardian`, with the selected candidate emitted in `edge.traversed`. Confirm changing the source stage auto-selects its active default target and that the route inventory lists all outgoing default/candidate routes. Current browser evidence for minimap-pan plus port drag: `artifacts/ui/runtime_ide_port_drag_after_minimap_v109.png`, where dragging `design:right` to `vision:left` keeps `graph.transitions.design=specimen`, adds `next_stage:vision`, marks the tab dirty, and disables saved-run buttons until save.
5. Export graph YAML, import the same YAML, validate, compile, and confirm no active graph is changed until `Save Version` is pressed. Current browser evidence: `artifacts/ui/runtime_ide_yaml_import_draft_v113.png`, where an imported YAML name change stays as an editor draft, `window.atrRuntimeIdeState.activeTabDirty=true`, saved-run buttons stay disabled, validate/dry-run operate on the draft payload, and `/api/graphs/atr_closed_loop` remains unchanged.
6. Load a module, change the module handler dropdown to another allowlisted handler and back, edit LLM role/backend/model, prompt path/system/developer overrides, tools, timeout/retry/safety flags, add an internal checkpoint and an executable agent step, duplicate one step, drag/drop reorder steps within a phase, confirm checkpoint steps stay checkpoints when the module handler changes, confirm Save Version is blocked before Apply Draft + Validate + Dry Run, then validate/dry-run before any save. Confirm Node Inspector Code Mapping displays graph/module/effective handler signatures and `runtime-callable` evidence from `/api/handlers`; current browser evidence: `artifacts/ui/runtime_ide_design_handler_signature_inspector_v115.png` selecting `design` with `agent.design_agent`, graph/effective signatures, and handler catalog rows visible; `artifacts/ui/runtime_ide_graph_first_layout_v119.png` confirms the graph canvas starts at y=470px after compacting the draft gate and closed launch panels. Current browser audit screenshots include `artifacts/ui/runtime_ide_browser_audit_evidence_vcd.png` for the `--- -> VC- -> VCD` evidence path, `artifacts/ui/runtime_ide_browser_audit_graph_switch.png` for graph canvas/minimap/JSON switching across all shipped graph templates, `artifacts/ui/runtime_ide_browser_audit_canvas_interactions.png` for node drag/snap, minimap pan, port edge creation, and `module:design` double-click tab opening, `artifacts/ui/runtime_ide_browser_audit_saved_test_run.png` for actual saved-test launch/timeline/canvas runtime evidence, and also include `artifacts/ui/runtime_ide_browser_audit_invalid_handler.png`, `artifacts/ui/runtime_ide_browser_audit_invalid_module.png`, and `artifacts/ui/runtime_ide_browser_audit_invalid_route.png` for negative readiness paths. The standalone Module Management Tool also has browser coverage: run `python3 tests/ui/module_management_browser_audit.py --base-url http://127.0.0.1:7860 --webdriver-url http://127.0.0.1:4448` to verify `/module-management` renders the real module catalog, selects the design module, exposes Module Designer controls, lists allowlisted handlers, and completes config Validate plus Dry Run. Current browser evidence is written to `artifacts/ui/module_management_browser_audit.png`. Current CUI smoke evidence should include `atr graph validate atr_closed_loop`, `atr graph compile atr_closed_loop`, `atr graph dry-run atr_closed_loop`, `atr graph gate atr_closed_loop`, `atr module validate design`, `atr module dry-run design`, `atr module load design`, and `atr module unload design`; these commands use the same saved active graph/module API as the browser launcher and Module Management workspace.
7. Start or inspect a run, confirm live node events, Agent Status, Device Status, Metrics, Human Approval Queue request/approve/reject flow, timeline focus, replay preview, and artifact preview/download continue to work. Also run BO and CAE workspace actions and confirm `/api/runs/{run_id}/artifacts` lists `workspace/bo/*_result.json`, `workspace/bo/*_bo_progress.svg`, `workspace/cae/*_result.json`, `workspace/cae/*.contour.svg`, and `workspace/cae/*.report.json`; the corresponding timeline events must expose `runtime_artifacts` and `artifact.created` payload paths so `Related Artifacts` can trace them. Browser coverage is available with `python3 tests/ui/runtime_ide_browser_audit.py --base-url http://127.0.0.1:7860 --webdriver-url http://127.0.0.1:4448 --scenario workspace-artifacts`, which writes `artifacts/ui/runtime_ide_browser_audit_workspace_artifacts.png` and checks BO SVG inline preview plus CAE contour/report grouping. Current saved-test browser evidence: `artifacts/ui/runtime_ide_run_saved_test_timeline_v111.png`, where `Run Saved Test` starts `atr_closed_loop`, Run ID updates, timeline receives `run.created`, `graph.compiled`, `run.started`, `edge.traversed`, `node.started`, and `module.step.*` events, and canvas classes show `idle` as visited plus `design` as active. Timeline/replay evidence: `artifacts/ui/runtime_ide_timeline_replay_detail_v112.png`, where selecting a timeline event fills Selected Event Detail, Event Log shows runtime rows with Inspect actions, Replay Preview runs a stage dry-run, displays replay validation, and highlights visited nodes plus the active selected node.
8. Exercise feature buttons/endpoints for the active GUI surfaces, including Runtime IDE graph controls, module validate/dry-run, draft graph/module edits, YAML export/import, state/events APIs, and workspace API controls, then debug every failure before merge.
9. Confirm Activation Checklist evidence cards show compiled graph counts, default routes, conditional candidate routes, full graph config fingerprint status, stage-dispatch mapping, dry-run digest/gate status, and dry-run rows containing `stage`, `next_stage`, `node_id`, `effective_handler`, and `module_runtime`-derived module counts where applicable.
10. Confirm graph compile/check failures and successes appear in the event stream as `graph.validation_failed` and `graph.compiled`, then debug any missing payload fields before merge. Confirm run preflight blocks every run mode when unsaved draft route/config changes or non-route graph config changes exist, and confirm live mode also blocks when the active dry-run gate is missing/stale or live confirmation is unchecked. Current automated evidence: `tests/ui/runtime_ide_browser_audit.py --scenario run-preflight`, screenshot `/tmp/atr_runtime_audit_run_preflight_v87/runtime_ide_browser_audit_run_preflight.png`, full-audit screenshot `/tmp/atr_runtime_audit_all_v89/runtime_ide_browser_audit_run_preflight.png`, `tests/ui/runtime_ide_browser_audit.py --scenario discard-draft` screenshot `/tmp/atr_runtime_audit_all_v89/runtime_ide_browser_audit_discard_draft.png`, and `tests/ui/runtime_ide_browser_audit.py --scenario save-version-lifecycle` screenshot `/tmp/atr_runtime_audit_all_v89/runtime_ide_browser_audit_save_version_lifecycle.png`.
11. Browser-check the saved-active execution invariant before merging UI changes: drag a main graph node, confirm `Run Saved Test`, `Run Saved Live`, and `Record Active Dry-run Gate` are disabled, validate and dry-run the draft without saving, and confirm run buttons remain blocked until `Save Version` activates the checked graph. Then open `design` as a module tab and use Module Management deep links or graph/node URLs to confirm module/node context opens without replacing the fixed Main System tab. Current reference screenshots: `artifacts/ui/runtime_ide_run_launcher_unsaved_draft_block_v104.png`, `artifacts/ui/runtime_ide_run_launcher_validated_unsaved_still_blocked_v104b.png`, and `artifacts/ui/runtime_ide_module_selector_tab_switch_v105.png`. Module-tab browser evidence: `artifacts/ui/runtime_ide_module_tab_step_dryrun_v110.png`, where double-clicking `design` opens `module:design`, dragging `01_intake_constraints` writes `metadata.position`, preflight blocks Save Version before exact validate/dry-run evidence, and `Module validation passed` / `Module dry-run passed` evidence appears in `Dry-run Trace`.
12. Open `/live` or run `python3 tests/ui/planning_browser_audit.py --base-url http://127.0.0.1:7860 --webdriver-url http://127.0.0.1:4448` and confirm Live GUI chat rendering shows `FEM / CAE Contour`, `BO Surrogate / Acquisition Trace`, a BO SVG, and selected candidate rows from plot-ready planning payloads. Current browser evidence is written to `artifacts/ui/planning_browser_audit_live_artifacts.png`.
13. Run `node --check web/static/runtime_ide.js web/static/planning.js web/static/module_management.js`, `python -m py_compile`, Runtime IDE unit tests, planning loop tests, full pytest, and `git diff --check`.
