# LeRobot Dataset, Policy, and Rollout Naming Rules

Scope: LeRobot / ROBOTIS teleoperation, recording, training, policy rollout, and Manipulation Agent integration.

## Naming Contract

Use three separate names for the three separate artifacts:

- Training dataset: human demonstration data recorded by teleoperation.
- Policy: trained model checkpoint or Hub policy repo.
- Rollout/evaluation dataset: robot execution data generated by a loaded policy.

Example:

```text
training dataset repo_id:  jin/pick_and_place_cube
policy repo_id:            jin/pick_and_place_cube_act
rollout dataset repo_id:   jin/eval_pick_and_place_cube_rollout_20260508T131538
```

## `eval_` Rule

`eval_` belongs on rollout/evaluation output datasets, not on training datasets.

Rules:

- Do not prefix teleoperation training datasets with `eval_`.
- Do not prefix policy repo IDs with `eval_`.
- Always prefix policy-generated rollout datasets with `eval_`.
- If an operator, GUI, or Manipulation Agent sends `dataset_repo_id=jin/pick_and_place_cube_rollout`, the bridge must execute LeRobot with `dataset_repo_id=jin/eval_pick_and_place_cube_rollout`.
- If the final repo name already starts with `eval_`, keep it unchanged.

Reason:

The installed LeRobot runtime uses `lerobot-record` for policy-controlled evaluation when a policy is provided. Its safety check requires the dataset name to begin with `eval_` to prevent policy-generated evaluation data from being confused with human demonstration training data.

## Local Path Rule

For rollout on this workstation, `dataset.root` is the final dataset directory, not only the parent cache directory.

Example command shape:

```text
--dataset.repo_id=jin/eval_pick_and_place_cube_rollout_20260508T131538
--dataset.root=/home/jin/.cache/huggingface/lerobot/jin/eval_pick_and_place_cube_rollout_20260508T131538
```

This matches the currently installed LeRobot implementation, where `LeRobotDataset.create(..., root=<path>)` creates the dataset directly at `<path>`.

## Manual-Stop Rollout Rule

Policy rollout should run until the operator stops it unless a duration is explicitly requested.

Rules:

- Empty GUI rollout duration means `continuous_rollout=true`.
- `continuous_rollout=true` maps to one long LeRobot episode with `episode_s=86400.0` and `num_episodes=1`.
- Pressing `Stop Rollout` terminates the active LeRobot rollout process group.
- If the GUI/user provides a rollout duration, set `continuous_rollout=false`, `episode_s=<duration>`, and `num_episodes=1`.

This is a pragmatic workaround because the installed LeRobot `lerobot-record --policy.path=...` path is episode-duration based and does not expose a native infinite policy-control mode.

## Safe Rollout Action Clamp

Rollout should enable per-step target clamping by default.

Rules:

- GUI default: `rollout_action_clamp=true`.
- GUI default limit: `rollout_max_relative_target=5`.
- Bridge command when enabled: `--robot.max_relative_target=5`.
- If disabled, the bridge omits `--robot.max_relative_target` and LeRobot falls back to the robot config default, currently `None` for `omx_follower`.

Purpose:

The clamp limits abrupt target jumps produced by a policy rollout. It does not fix a bad policy or camera/pose mismatch, but it reduces sudden large per-step direction changes and makes early rollout validation safer.

## Pi0.5 Training Runtime

Pi0.5 support is intentionally isolated from the ROBOTIS OMX live runtime.

Rules:

- Default robot runtime: `/home/jin/lerobot` in conda env `lerobot`.
- Pi0.5 training runtime: `/home/jin/lerobot_pi05` in conda env `lerobot-pi05`.
- Pi0.5 Hugging Face cache: `HF_HOME=/home/jin/.cache/huggingface_pi05`, `HF_HUB_CACHE=/home/jin/.cache/huggingface_pi05/hub`, `HF_HUB_DISABLE_XET=1`.
- GUI policy type: `pi05`.
- Default HF source policy: `lerobot/pi05_base`.
- Bridge command for Pi0.5 training uses `conda run --no-capture-output -n lerobot-pi05 lerobot-train`.
- The Pi0.5 source model is passed as `--policy.pretrained_path=lerobot/pi05_base`; this differs from the older LeRobot 0.3.x `--policy.path=<repo-or-dir>` path used by some non-Pi0.5 fine-tuning flows.
- Pi0.5 live training requires LeRobot dataset format `v3.0`. If the selected local training dataset is `v2.1`, the bridge must not mutate the original ROBOTIS/LeRobot recording. It must copy the dataset to `~/.cache/huggingface/lerobot/local-pi05-v30/<dataset-slug>`, run the v2.1 -> v3.0 converter on that generated copy, then train against the converted repo id/path.
- Generated Pi0.5 v3.0 datasets are excluded from generic "latest local dataset" selection unless the operator explicitly selects the `local-pi05-v30/...` repo id. This prevents ACT training from accidentally picking a Pi0.5-only converted dataset.

Prefetch command:

```bash
mkdir -p /home/jin/.cache/huggingface_pi05
HF_HOME=/home/jin/.cache/huggingface_pi05 HF_HUB_CACHE=/home/jin/.cache/huggingface_pi05/hub HF_HUB_DISABLE_XET=1 conda run -n lerobot-pi05 hf download lerobot/pi05_base --max-workers 1
```

Recommended additional train args for local LeRobot datasets that do not include Pi0.5 quantile stats:

```text
--policy.compile_model=true
--policy.gradient_checkpointing=true
--policy.dtype=bfloat16
--policy.freeze_vision_encoder=false
--policy.train_expert_only=false
--policy.normalization_mapping={"ACTION":"MEAN_STD","STATE":"MEAN_STD","VISUAL":"IDENTITY"}
```

Manual local conversion command shape:

```bash
conda run --no-capture-output -n lerobot-pi05 python -m lerobot.datasets.v30.convert_dataset_v21_to_v30 \
  --repo-id=local-pi05-v30/jin-record-test-20260512t063639z \
  --root=/home/jin/.cache/huggingface/lerobot \
  --push-to-hub=false \
  --force-conversion
```

`--root` is the LeRobot dataset cache parent. The converter appends `--repo-id` internally, so do not pass the final dataset directory as `--root`.

Policy-specific GUI defaults:

- ACT follows the Hugging Face LeRobot train defaults and ACT config: `batch_size=8`, `steps=100000`, `num_workers=4`, `eval_freq=20000`, `log_freq=200`, `save_freq=20000`, `policy.n_obs_steps=1`, `policy.chunk_size=100`, `policy.n_action_steps=100`.
- Pi0.5 follows the Hugging Face Pi0.5 fine-tuning example and Pi0.5 config: `batch_size=32`, `steps=3000`, `policy.n_obs_steps=1`, `policy.chunk_size=50`, `policy.n_action_steps=50`, plus `compile_model`, `gradient_checkpointing`, `dtype=bfloat16`, `freeze_vision_encoder=false`, and `train_expert_only=false`.
- Official references: https://huggingface.co/docs/lerobot/act and https://huggingface.co/docs/lerobot/pi05.

## ACT Temporal Ensemble Filter

Rollout should enable ACT temporal ensembling by default for smoother policy actions.

Rules:

- GUI default: `rollout_temporal_ensemble=true`.
- GUI default coefficient: `rollout_temporal_ensemble_coeff=0.01`.
- Bridge command when enabled: `--policy.temporal_ensemble_coeff=0.01`.
- Bridge must also add `--policy.n_action_steps=1`, because the installed LeRobot ACT config requires `n_action_steps=1` when temporal ensembling is enabled.
- If disabled, the bridge must omit both `--policy.temporal_ensemble_coeff` and the rollout-specific `--policy.n_action_steps=1` override.

Purpose:

Temporal ensembling averages overlapping ACT action chunks across time. It reduces chunk-boundary jitter and policy uncertainty jitter. It is not a motor speed limit and does not replace correct starting pose, camera mapping, or dataset quality.

## Manipulation Agent Reuse

Manipulation Agent must not implement its own LeRobot naming logic.

Rules:

- Manipulation Agent may provide `rollout_dataset_repo_id`, `lerobot_rollout_dataset_repo_id`, or `dataset_repo_id`.
- Manipulation Agent may provide `continuous_rollout`.
- Manipulation Agent may provide `rollout_action_clamp` and `rollout_max_relative_target`.
- Manipulation Agent may provide `rollout_temporal_ensemble` and `rollout_temporal_ensemble_coeff`.
- `device_bridges/lerobot_bridge.py` remains the single enforcement point for `eval_` naming, local policy-path normalization, and manual-stop rollout conversion.
- This keeps GUI rollout and autonomous Manipulation Agent rollout consistent.

## Pi0.5 Transfer Rollout Rule

When the workflow reaches Manipulation after a ready 3DP specimen, the default robot-transfer policy is Pi0.5 LeRobot rollout unless the experiment spec explicitly requests `fixed_kinematic`.

Rules:

- Manipulation Agent sends `policy_type=pi05`.
- Test mode may use `policy_path=fake://pi05_policy`.
- Live mode requires a real `policy_path`, `policy_checkpoint_path`, or `policy_repo_id`.
- The LeRobot bridge runs Pi0.5 rollout in conda env `lerobot-pi05`.
- Pi0.5 rollout command preview uses `lerobot-rollout`, `--policy.type=pi05`, `--device=<device>`, and `--inference.type=rtc` by default.
- The task text must mention the concrete transfer target: `3dp_output_area -> utm_fixture`.

References:

- https://huggingface.co/docs/lerobot/main/en/inference
- https://huggingface.co/docs/lerobot/pi05
