Edit the supplied architecture figure. Preserve the excellent typography, navy/teal/violet palette, professional editorial panels, main title, right-hand responsibility model, and bottom interface modules. Fix the following structural issues precisely.

Replace the ENTIRE contents inside panel 01 Orchestration Plan with an abstract, non-sequential configured task graph. This panel is the reusable coordination abstraction, not a particular experiment. Remove all the current stage nodes, FEM, duplicated arrows and staircase. Use a clean left-to-right diamond-shaped directed acyclic topology that branches and joins. Node at left labelled "Task A". It branches into "Task B" at upper-middle and "Task C" at lower-middle. Both B and C connect to "Task D" at right-middle; between their outgoing edges put a tiny annotation "Dependencies". D connects to a diamond "Review" at right. Review has one edge labelled "Accept" leading to a compact "Result" node at far right. A dashed teal edge labelled "Revise" returns from Review to Task A around the upper perimeter. This return edge must not overlap Orchestrator's input arrow. Orchestrator input arrow points to Task A. B and C are separate branches; add the small annotation "Independent work" in the whitespace between them. Exactly these nodes and edges, no extras. Beneath the graph inside panel 01, a neatly aligned two-row catalog labelled "Specialist agents" contains seven compact pills Design, Specimen, Vision, Manipulation, Equipment, Analysis, BO. A single dotted vertical leader from Task C down to the catalog is labelled "Task assignment". The catalog is NOT a chain and has NO arrows between pills. Use its space fully and keep labels readable. Tiny caption bottom right of panel: "Conceptual plan; routes are configured". Legend bottom left: solid navy "Dependency", dashed teal "Feedback", dotted gray "Assignment". One single dotted magnification leader from Task D to the right panel 02 denotes the task's agent structure. Remove any other diagonal leader from main plan to LLM.

Fix panel 02 action and evidence directions: exactly one downward arrow from High to Middle and one from Middle to Low for Action; exactly one upward arrow Low to Middle and one Middle to High for Evidence. Keep the High/Middle/Low labels and LLM <-> Tools pair. Keep the LLM-tools pair as an internal inset, not an extra control level. Both Guardian / Safety and Knowledge / Evidence shared ribbons must span the full width below both upper panels. Below them place a clean vertical Commands arrow down from the Low side to Registered tools and device bridges, and separate Observations arrow up from that rail to the Low side. These two arrows cross the shared ribbons using a clear visual corridor and terminate on the bridge rail, NOT the knowledge ribbon. Keep the four integration modules.

Preserve the rest of the image. Make the overall organization clearly a professional FRAMEWORK ARCHITECTURE rather than a fixed closed-loop experiment. No fabricated numerical results, no added hardware illustrations. All text English. Carefully inspect all arrow endpoints and exact labels.
