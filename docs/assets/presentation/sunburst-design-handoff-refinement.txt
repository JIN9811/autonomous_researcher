Edit only the far-right endpoint in the supplied Design Agent figure. It currently shows a physical block labeled Specimen, which can be mistaken for a fabricated object. Replace that endpoint icon with a small agent processor-with-pins pictogram and label it exactly "Specimen Agent" on one or two readable lines. The arrow from Design candidate must terminate at this agent icon. Keep every other icon, arrow, text label, position, color, line weight and the white background unchanged. Do not alter the suitability decision, candidate checks, inspection evidence loop, owner-review branch or source parameters. This is a software handoff to the fabrication agent, not a physically made specimen.
