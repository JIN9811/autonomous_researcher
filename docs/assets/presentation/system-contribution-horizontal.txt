Use case: infographic-diagram / layout redesign. Input image is the edit target: AX4LAB System Contribution. Preserve ONLY its semantic contributions and navy/cyan/white Sunburst paper-figure palette; REPLACE the tall triangular layout, hardware pictograms and long connections entirely. Render a NEW very wide, shallow horizontal scientific figure, aspect ratio 3:1, ideally 1536 by 512 pixels. Do not keep the input's tall canvas and do not letterbox a thin diagram inside a tall image. Three equal-width compact box-like modules sit side-by-side on one baseline, with 24-pixel gaps and small outer margins. Modules have fine navy outlines, subtly rounded corners, white interiors, and a short cyan top rule. No enclosing box around all three. No title above or footer below. No realistic or illustrated robot arm, chip, laboratory, machine, bench, equipment icons, brain, 3D, shadows or gradient. This is a concise abstract system-contribution schematic for a paper, with readable geometric sans-serif text, not a picture of hardware.

LEFT module exact heading, on two lines if needed: 'Hierarchical Automation'. Under it draw three small horizontal rectangular blocks stacked vertically with short downward connectors and exact labels 'High: decisions', 'Middle: procedures', 'Low: tools'. At the bottom, one short aligned line 'Device Bridges'. Keep the entire motif compact.

CENTER module exact heading, on two lines if needed: 'Multi-Agent Coordination'. Under it draw four small equal rectangular nodes labeled 'Agent' in an open branching/joining graph, not a ring and not one serial chain. At the bottom, one short aligned line 'Configurable orchestration'. Avoid any extra invented labels.

RIGHT module exact heading, on two lines if needed: 'VLA-Based Manipulation'. Under it draw three small rectangular blocks stacked vertically with short downward connectors and exact labels 'Task instruction', 'VLA policy', 'Robot interface'. At the bottom, one short aligned line 'LeRobot integration'. No robotic arm pictogram. This is a capability-level abstraction, not a claim of a new runtime execution path.

The THREE MODULES are PARALLEL complementary contributions: do not draw arrows from the first module to the second or from the second to the third. Equal visual weight, equal card heights, matching heading baselines and footer baselines, comfortable readable text with minimal words. Keep the final canvas shallow so the image occupies little vertical space above a Markdown table. No figure number, no legend, no AX4LAB logo, no performance or savings claims. All supplied English must be spelled exactly. White background edge-to-edge.
