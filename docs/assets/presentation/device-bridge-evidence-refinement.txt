Use case: precise-object-edit. Input image 1 is the edit target, the AX4LAB Device Bridge paper figure. Preserve the wide composition, all exact English labels, navy/cyan colors, pictograms, forward cyan connections and white background. Change only the navy dashed evidence-return routing: instead of emerging solely from PC-operated instruments, it must collect separately from the right edge of EACH of the three peer capability blocks (Robotics, API-controlled devices, PC-operated instruments). Join these three short dashed stubs into one vertical dashed collector in the right margin, then continue below the blocks onto the existing bottom dashed return arrow pointing back to Specialist agents. Keep the label 'Status, observations, artifacts' once under that return. Add right margin if necessary, do not cross any text, and do not connect the three blocks as a sequential execution chain. Keep all other labels and layout unchanged.
