Use case: precise-object-edit
Edit the supplied AX4LAB research framework figure. Keep its pictogram designs, navy/cyan flat palette, typography, labels and semantic connection topology. Make ONLY the following requested layout, in-figure logo and footer changes. Output a complete polished 1536 x 1024 landscape figure on pure white.

1. STAGGERED DIAGONAL LAYOUT. The current Existing Laboratory and Multi-Agent AI are almost horizontally aligned, and the Device Bridge connection is horizontal. Change this into a visibly descending zigzag: Existing Laboratory at upper LEFT, Multi-Agent AI Orchestration at RIGHT approximately half a diagram row LOWER, then Device Bridge at CENTER approximately another half-row LOWER than the AI. This is spatial staggering, NOT a change to the control architecture. The AI-to-bridge bidirectional connector must visibly run diagonally down-left toward the bridge, NOT horizontally. Keep the three equipment categories beneath the bridge, connected individually to it. To achieve this without crowding the footer, move the lab upward into the space freed by removal of the logo symbol and moderately reduce the AI chip if necessary. Suggested relative ICON center heights (not label baselines): lab about y=230, AI chip about y=380, Device Bridge about y=530, equipment about y=735. Lab stays at left x~300, AI at right x~1250, Bridge at center x~780. Labels remain directly beneath their icons with clear margins. Keep the existing wide curved transformation arrow going from the lab toward the AI, now descending toward the lower AI position, with no explanatory text on the arrow. Retain comfortable negative space and safe outer margins. Do NOT add a second lab, second bridge, extra equipment, numbered steps, or duplicate arrows.

2. IN-FIGURE LOGO ONLY. Remove the entire illustrated robot-arm/large-4/chip/orbit symbol above the AX4LAB wordmark. Leave ONLY the original stylized wordmark AX4LAB (navy letters with cyan 4), positioned above the transformation arrow. Preserve that wordmark's lettering and proportions. The now-empty top area should be used to balance the staggered composition, not left as a huge blank gap. This deletion applies only to the logo emblem, NOT the AI-native robot pictogram below, and NOT the AI processor icon on the right.

3. FOOTER: Replace the three centered text lines with a three-item bullet list. Center the entire list block as a unit at the bottom of the canvas, but LEFT-ALIGN the bullets to ONE shared vertical column and LEFT-ALIGN the start of all three sentences to a second shared vertical column. Do NOT center each individual line. Use small plain round bullets, identical in size and navy color, with even line spacing. No icons, checkmarks, separators, or bounding box. Exact text, each on one line:
• Reuse existing laboratory equipment
• From low-cost to general-purpose hardware
• Multi-agent-based self-driving lab transformation
Make the first two lines regular weight and the third medium weight; keep the bullets aligned even if the third is longer. Leave a comfortable gap after equipment labels and at the bottom edge. Remove the old wording high-end hardware completely.

Invariants: Only one lab pictogram, one AX4LAB wordmark, one AI chip, one Device Bridge, and exactly three equipment pictograms labeled AI-native Equipment, API-enabled Equipment, PC-operated Equipment. Every equipment connector goes through Device Bridge. Preserve the labels Multi-Agent AI and Orchestration. No new explanatory words, no Structured AI layers, no gradients/glow/3D, no enclosing panels. Crisp, readable scientific editorial layout, not a marketing poster.
