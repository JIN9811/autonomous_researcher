# Local Memory Files

This directory stores local operator/device memory such as printer credentials,
Windows bridge endpoints, robot port mappings, and temporary resume notes.

Contents are ignored by Git because they can contain secrets, IP addresses,
hardware-specific state, or machine-local calibration data.

Keep only documentation or non-secret templates under version control.
