# User Files

Use this directory for local user-provided datasets, private experiment inputs,
temporary screenshots, and machine-specific files that should not be committed.

Contents are ignored by Git by default. If a file should become part of the
system, move it to the appropriate tracked directory after removing secrets and
large generated data.
